启动当前项目入口独享的本地 Web 控制台。

用法:
  {{INVOCATION}}

对于原生 EXE Entry，该命令等价于不带参数启动 {{COMMAND}}，并且在首次
Profile 尚未保存时也可使用。它通过当前 Entry 的零参数启动路径拉起 Host，
不会维护第二套 Host 启动逻辑。

Web Host 只监听
127.0.0.1 随机端口，自动打开系统默认浏览器，并常驻系统托盘；关闭浏览器
页面不会停止服务，只有托盘菜单“退出”才会完整停止。

当前 V0 尚未实现同一入口的单实例复用，请勿重复启动。
