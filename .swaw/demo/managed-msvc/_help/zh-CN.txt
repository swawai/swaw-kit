演示由命令模块自行声明并检查“仅使用项目受管 MSVC”前提。

用法:
  {{INVOCATION}}

该模块展示框架的去中心化前提协议：
  1. Core 只激活并验证属于当前项目的环境。
  2. 命令模块读取 SWAWKIT_DEV_MSVC_*，自行决定工具来源策略。
  3. managed-only 模块还要验证实际工具位于受管 MSVC_HOME 内，
     不能因系统 PATH 中存在同名工具而静默降级。

允许使用系统工具的模块可以直接解析 PATH；优先受管但允许系统
后备的模块，也应在自己的入口中明确实现并说明这一策略。
